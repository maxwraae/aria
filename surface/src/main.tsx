import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('ARIA', () => App);
AppRegistry.runApplication('ARIA', {
  rootTag: document.getElementById('root'),
});
